
module Masked.Prelude
       ( module Exports
       ) where

-- from http://www.stephendiehl.com/what/#prelude
import           Prelude as Exports hiding (all, and, any, concat,
                                     concatMap, head, map, mapM,
                                     mapM_, read, sequence, sequence_,
                                     (!!))
